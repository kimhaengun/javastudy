<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.douzone</groupId>
  <artifactId>javastudy</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

	<!-- UTF-8 설정 -->
	<properties>
		<project.build.sourceEncodind>UTF-8</project.build.sourceEncodind>
		<project.reporting.outputEncodind>UTF-8</project.reporting.outputEncodind>
	</properties>

	<!-- 의존성 주입 -->
	<dependencies>
		
	</dependencies>

	<!-- Maven 빌드 경로 -->
	<build>
		<sourceDirectory>src/main/java</sourceDirectory>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.8.0</version>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
				</configuration>
			</plugin>
		</plugins>
	</build>  
  
	<modules>
		<module>practice01</module>
		<module>chapter03</module>
	</modules>
</project>